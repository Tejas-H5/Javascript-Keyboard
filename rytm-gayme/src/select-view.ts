import { RenderContext } from "./render-context";
import { div, RenderGroup } from "./utils/dom-utils";

export function SelectView(rg: RenderGroup<RenderContext>) {
    return div({ class: "flex-1 col" }, [
        "TODO: implement"
    ]);
}
